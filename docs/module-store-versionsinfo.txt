1.0 build 36
geändert:
- Verbunden und Störung sind jetzt String-Variablen mit farbiger Wertedarstellung
  (wie Status) statt einfacher Ja/Nein-Werte. Werden beim Update einmalig neu angelegt.

1.0 build 35
korrigiert:
- Szenen setzten teils nur den ersten Wert (z.B. nur Ein/Aus, aber nicht Solltemperatur/
  Lüfter). Mehrere schnelle Steuerbefehle werden jetzt zu einem Befehl zusammengefasst.
- Status blieb dauerhaft auf dem befohlenen Modus stehen (z.B. immer "Kühlen") statt
  "Leerlauf" zu zeigen, wenn die Solltemperatur bereits erreicht ist.

1.0 build 34
korrigiert:
- Configurator zeigte teils dauerhaft "Keine Verbindung zum Splitter", obwohl die
  Verbindung bestand. Status wird jetzt auch beim Öffnen der Konfigurationsseite geprüft.

1.0 build 33
geändert:
- Dokumentation überarbeitet (README je Modul auf aktuellen Stand gebracht).

1.0 build 32
neu:
- Solltemperatur-Bereich wird jetzt je Betriebsmodus dynamisch vom Gerät übernommen (z.B.
  Heizen bis 10 °C statt fix 16 °C, falls unterstützt).

1.0 build 31
korrigiert:
- Verbunden folgt jetzt dem echten Verbindungsstatus des Geräts statt fest "verbunden"
  anzuzeigen.
- API-Diagnose zeigt Energie-Telemetrie-Kennzahlen jetzt korrekt an.

1.0 build 30
neu:
- Testknopf "API-Diagnose ausführen" im Connection-Modul: prüft /context, Energie- und
  Trendsummary-Endpunkt auf ungenutzte Felder und meldet das Ergebnis als Übersicht.

1.0 build 29
korrigiert:
- Alle Statusvariablen (Power, Vane vertikal/horizontal, Raumtemperatur, Außentemperatur,
  Verbunden, Störung, WLAN-Signal, Energieverbrauch) zeigen jetzt Icons, Farben und
  Einheiten exakt wie in der manuellen Symcon-Konfiguration.

1.0 build 28
korrigiert:
- Fehlende Einheiten (kWh, °C) bei Energieverbrauch und Außentemperatur behoben.

1.0 build 27
korrigiert:
- Verbleibende Legacy-Variablenprofile (Verbunden, WLAN-Signal, Energieverbrauch) durch
  Variable Presentations ersetzt.

1.0 build 26
korrigiert:
- Außentemperatur-Abruf korrigiert (listenverpackte Cloud-Antwort wird jetzt richtig
  ausgepackt).

1.0 build 25
neu:
- Testknopf "Alle Daten abrufen" im Connection-Modul (ruft Status und Energie inkl.
  Außentemperatur für alle Geräte ab).

1.0 build 24
korrigiert:
- Temperaturverlauf (Farbverlauf) am Solltemperatur-Schieberegler wiederhergestellt.

1.0 build 23
korrigiert:
- Legacy-Profil an der Raumtemperatur durch moderne Variable Presentation ersetzt.

1.0 build 22
korrigiert:
- Solltemperatur-Schieberegler nach fehlgeschlagenem Zwischenschritt wieder repariert.

1.0 build 21
neu:
- Außentemperatur-Statusvariable ergänzt.

1.0 build 20
korrigiert:
- Zulässiger Bereich der Solltemperatur auf 16-31 °C korrigiert.

1.0 build 19
neu:
- Eigene Wertedarstellung für die Störungsvariable (anpassbare Beschriftung statt
  "OK"/"Alarm").

1.0 build 18
geändert:
- Modulstore-Veröffentlichung vorbereitet.

Ältere Historie: siehe CHANGELOG.md im Repository.
